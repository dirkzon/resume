to start local development: `docker-compose up`

to stop local development: `docker-compose down`

Includes live reloading of page (`index.md`), but not config.
To reload config: `docker-compose restart jekyll`